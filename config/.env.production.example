## Enddel SSR server production configuration
# Скопируйте этот файл в .env перед деплоем и подставьте боевые значения.

# Базовый URL API каталога. Может указывать на staging/production backend.
PRODUCT_API_BASE=https://enddel.com/api

# Размер ленивого LRU-кэша (количество продуктов с тяжёлыми данными).
# Увеличьте, если есть запас памяти и наблюдаются частые промахи.
PRODUCT_LAZY_CACHE_CAPACITY=256
